# Distributed-Shopping-System
A client-server application where a sender process establishes a TCP connection with middle server with credentials which will be validated by middle server machine upon which it connects client to appropriate group server
